 <h1>Edit Wiki</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for wikis:</p>
     <ul>
       <li>Create clear,concise and relevant wikis.</li>
       <li>Ensure that the wiki content is truthful and accurate.</li>
       <li>Omit profane and obscene words.</li>
     </ul>
     <% if permit_collaboration?(current_user) %>
       <%= render partial: 'dropdown' %><br>
       <p style="font-size: 17px">Remove collaborators</p>
       <% @wiki.collaborators.each do |collaborator| %>
       <div class="row pull-left">
         <%= collaborator.user.email %>
         <%= link_to 'Remove', collaborator_path(collaborator), data: { confirm: 'Are you sure?' }, :style => "margin-left: 20px", method: :delete %>
       </div class="row">
       <% end %>
     <% end %>
   </div>
   <%= render partial: 'wikis/form', locals: { wiki: @wiki } %>
 </div>